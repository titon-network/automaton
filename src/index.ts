// Library entry point — re-exports types/helpers for programmatic use.
// Populated incrementally across Phase D tasks. Kept minimal at scaffold
// time so `tsc` has something to compile and `package.json` main resolves.

export const AUTOMATON_SCAFFOLD_VERSION = 1;
